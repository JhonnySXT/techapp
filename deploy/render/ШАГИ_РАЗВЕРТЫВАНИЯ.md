# 🚀 Пошаговый план развертывания на Render.com

## ✅ Шаг 1: Проверка готовности проекта

### Что нужно проверить:

- [x] ✅ Файлы развертывания созданы в `deploy/render/`
- [ ] ⬜ Проект на GitHub (или готов к загрузке)
- [ ] ⬜ Аккаунт на Render.com
- [ ] ⬜ Аккаунт на GitHub (если нет проекта)

---

## 📋 Шаг 2: Подготовка проекта для GitHub

### Вариант A: Если Git уже установлен

1. **Проверить статус:**
   ```bash
   git status
   ```

2. **Если репозиторий не инициализирован:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Techapp server ready for deployment"
   ```

3. **Создать репозиторий на GitHub:**
   - Перейдите на https://github.com/new
   - Название: `techapp` (или любое другое)
   - Выберите Public или Private
   - НЕ добавляйте README, .gitignore или лицензию (они уже есть)
   - Нажмите "Create repository"

4. **Подключить и загрузить:**
   ```bash
   git remote add origin https://github.com/ваш-username/techapp.git
   git branch -M main
   git push -u origin main
   ```

### Вариант B: Если Git не установлен

**Установите Git:**
1. Скачайте: https://git-scm.com/download/win
2. Установите (можно оставить все настройки по умолчанию)
3. Перезапустите терминал/Android Studio

**Или используйте GitHub Desktop:**
1. Скачайте: https://desktop.github.com/
2. Установите
3. Войдите в аккаунт GitHub
4. Создайте новый репозиторий через интерфейс

---

## 🌐 Шаг 3: Регистрация на Render.com

1. **Перейдите на:** https://render.com

2. **Нажмите:** "Get Started for Free"

3. **Выберите регистрацию через GitHub:**
   - Нажмите "Continue with GitHub"
   - Разрешите доступ Render к вашему GitHub аккаунту
   - Это самый простой способ

4. **Подтвердите email:**
   - Проверьте почту
   - Нажмите на ссылку подтверждения

5. **Готово!** Вы в панели Render Dashboard

---

## 🚀 Шаг 4: Создание Web Service на Render

### 4.1. Начало создания

1. В панели Render нажмите **"New +"** (вверху справа)
2. Выберите **"Web Service"**

### 4.2. Подключение репозитория

1. Если GitHub еще не подключен:
   - Нажмите "Connect account"
   - Выберите "GitHub"
   - Разрешите доступ
   - Выберите репозиторий `techapp`

2. Если уже подключен:
   - Выберите репозиторий из списка

### 4.3. Настройка сервиса

**Основные настройки:**

- **Name:** `techapp-server` (или любое имя)
- **Environment:** `Docker` ⚠️ **ВАЖНО!**
- **Region:** Выберите ближайший:
  - `Frankfurt (EU)` - для Европы/России
  - `Oregon (US West)` - для США
  - `Singapore (AP Southeast)` - для Азии
- **Branch:** `main` (или ваша ветка)
- **Root Directory:** оставьте **пустым**
- **Dockerfile Path:** `deploy/render/Dockerfile` ⚠️ **ВАЖНО!**
- **Docker Build Context:** `.` (точка - корень проекта) ⚠️ **ВАЖНО!**
- **Docker Command:** оставьте **пустым**

### 4.4. Переменные окружения

Нажмите **"Advanced"** и добавьте переменные:

1. **PORT:**
   - Key: `PORT`
   - Value: `8081` (или оставьте пустым - Render назначит автоматически)

2. **HOST:**
   - Key: `HOST`
   - Value: `0.0.0.0`

### 4.5. План

- Выберите **"Free"** для начала (можно изменить позже)

### 4.6. Создание

Нажмите **"Create Web Service"**

---

## ⏳ Шаг 5: Ожидание деплоя

Render начнет процесс:

1. **Клонирование репозитория** (1-2 минуты)
2. **Сборка Docker образа** (5-8 минут)
   - Установка зависимостей
   - Компиляция Kotlin
   - Сборка JAR файла
   - Создание Docker образа
3. **Развертывание** (1-2 минуты)
4. **Запуск сервера** (30 секунд)

**Итого:** 8-12 минут

Вы увидите логи в реальном времени. Дождитесь сообщения:
```
Your service is live at https://techapp-server.onrender.com
```

---

## ✅ Шаг 6: Проверка работы

### 6.1. Health Check

Откройте в браузере:
```
https://techapp-server.onrender.com/health
```

Должно показать: `{"status":"ok"}`

### 6.2. Проверка авторизации

Используйте Postman или curl:

```bash
curl -X POST https://techapp-server.onrender.com/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d "{\"email\":\"admin@techapp.local\",\"password\":\"admin123\"}"
```

Должен вернуться JSON с токеном.

---

## 📱 Шаг 7: Настройка Android приложения

После успешного развертывания получите URL вида:
```
https://techapp-server.onrender.com
```

**API URL:**
```
https://techapp-server.onrender.com/api/v1
```

Используйте этот URL в Android приложении.

---

## ⚠️ Возможные проблемы и решения

### Проблема 1: "Dockerfile not found"

**Решение:**
- Убедитесь, что путь правильный: `deploy/render/Dockerfile`
- Проверьте, что файл загружен на GitHub

### Проблема 2: "Build failed"

**Решение:**
- Проверьте логи в Render Dashboard
- Убедитесь, что все зависимости указаны в `build.gradle.kts`
- Проверьте, что версия Gradle совпадает (8.13)

### Проблема 3: "Service not responding"

**Решение:**
- Проверьте переменные окружения (PORT, HOST)
- Проверьте логи сервера
- Убедитесь, что сервер запустился (зеленый индикатор)

### Проблема 4: "Connection timeout"

**Решение:**
- На бесплатном плане сервер "засыпает" после 15 минут
- Первый запрос после пробуждения может занять 30-60 секунд
- Это нормально для бесплатного плана

---

## 🎉 Готово!

После успешного развертывания:

1. ✅ Сервер доступен из интернета
2. ✅ Android приложение может работать откуда угодно
3. ✅ Автоматический деплой при каждом push в GitHub

---

## 📝 Следующие шаги

1. **Измените пароль администратора** (важно для безопасности)
2. **Протестируйте все функции** через Android приложение
3. **Настройте домен** (опционально, для красивого URL)
4. **Настройте резервное копирование БД** (для продакшена)

---

## 💡 Полезные ссылки

- **Render Dashboard:** https://dashboard.render.com
- **Документация Render:** https://render.com/docs
- **Логи сервера:** Доступны в Render Dashboard → ваш сервис → Logs

---

**Готовы начать?** Начните с Шага 1! 🚀










