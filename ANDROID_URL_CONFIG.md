# 🔧 Конфигурация URL сервера для Android приложения

## 📍 Где находится URL сервера

### Текущая конфигурация сервера:
- **Файл:** `server/src/main/resources/application.conf`
- **Порт:** `8081`
- **Хост:** `0.0.0.0` (доступен извне)

### Базовый URL API:
```
http://localhost:8081/api/v1
```

---

## 🎯 Какой URL использовать для Android приложения?

### Вариант 1: Эмулятор Android Studio (РЕКОМЕНДУЕТСЯ для разработки)

**URL:** `http://10.0.2.2:8081/api/v1`

- `10.0.2.2` - это специальный адрес, который эмулятор использует для доступа к `localhost` вашего компьютера
- Работает автоматически, ничего настраивать не нужно
- Используйте этот вариант для тестирования в эмуляторе

### Вариант 2: Реальное Android устройство (в той же Wi-Fi сети)

**URL:** `http://[IP_вашего_компьютера]:8081/api/v1`

**Как узнать IP адрес:**
1. **Через PowerShell (Windows):**
   - Откройте PowerShell
   - Введите: `ipconfig`
   - Найдите "IPv4 Address" (обычно начинается с 192.168.x.x или 10.x.x.x)
   - Или запустите скрипт: `.\get-ip.ps1`

2. **Через командную строку:**
   - Нажмите `Win + R`, введите `cmd`
   - Введите: `ipconfig`
   - Найдите "IPv4 Address"

3. **Пример:**
   - Если IP адрес: `192.168.1.100`
   - URL будет: `http://192.168.1.100:8081/api/v1`

**Важно:**
- Компьютер и Android устройство должны быть в одной Wi-Fi сети
- Убедитесь, что брандмауэр Windows разрешает подключения на порт 8081

### Вариант 3: Продакшен (когда сервер будет на хостинге)

**URL:** `http://ваш-домен.com/api/v1` или `http://ваш-ip:8081/api/v1`

- Используйте, когда сервер будет развернут на хостинге
- Нужно будет настроить домен и SSL сертификат

---

## 🔧 Как настроить URL в Android приложении

### Способ 1: Через BuildConfig (РЕКОМЕНДУЕТСЯ)

В `app/build.gradle.kts` добавим:

```kotlin
android {
    defaultConfig {
        // ... существующие настройки
        
        // URL для разработки (эмулятор)
        buildConfigField("String", "API_BASE_URL", "\"http://10.0.2.2:8081/api/v1\"")
        
        // Или для реального устройства (замените на ваш IP)
        // buildConfigField("String", "API_BASE_URL", "\"http://192.168.1.100:8081/api/v1\"")
    }
    
    buildTypes {
        debug {
            // URL для разработки
            buildConfigField("String", "API_BASE_URL", "\"http://10.0.2.2:8081/api/v1\"")
        }
        release {
            // URL для продакшена
            buildConfigField("String", "API_BASE_URL", "\"https://ваш-домен.com/api/v1\"")
        }
    }
}
```

Использование в коде:
```kotlin
val apiUrl = BuildConfig.API_BASE_URL
```

### Способ 2: Через strings.xml

В `app/src/main/res/values/strings.xml`:
```xml
<string name="api_base_url">http://10.0.2.2:8081/api/v1</string>
```

Использование:
```kotlin
val apiUrl = context.getString(R.string.api_base_url)
```

### Способ 3: Через отдельный конфигурационный файл

Создать `app/src/main/assets/config.properties`:
```properties
api.base.url=http://10.0.2.2:8081/api/v1
```

---

## 🚀 Быстрый старт

### Для разработки (эмулятор):
```
http://10.0.2.2:8081/api/v1
```

### Для тестирования на реальном устройстве:
1. Узнайте IP адрес компьютера (запустите `get-ip.ps1`)
2. Используйте: `http://[ваш-ip]:8081/api/v1`
3. Убедитесь, что устройство в той же Wi-Fi сети

---

## ⚠️ Важные замечания

1. **Брандмауэр Windows:**
   - Может блокировать подключения на порт 8081
   - Если не работает, добавьте правило в брандмауэре для порта 8081

2. **Wi-Fi сеть:**
   - Компьютер и Android устройство должны быть в одной сети
   - Некоторые публичные Wi-Fi могут блокировать прямое подключение

3. **HTTPS:**
   - Для продакшена обязательно используйте HTTPS
   - Для разработки HTTP подойдет

---

## 📝 Примеры полных URL

### Авторизация:
```
POST http://10.0.2.2:8081/api/v1/auth/login
```

### Получить список заявок:
```
GET http://10.0.2.2:8081/api/v1/tickets
Headers: Authorization: Bearer {token}
```

### Создать заявку:
```
POST http://10.0.2.2:8081/api/v1/tickets
Headers: Authorization: Bearer {token}
```

---

## 🔍 Проверка доступности сервера

### С компьютера:
```bash
curl http://localhost:8081/health
```

### С Android устройства (в браузере):
```
http://[IP_компьютера]:8081/health
```

Должен вернуться: `{"status":"ok"}`

---

## 💡 Рекомендация

**Для начала разработки используйте:**
```
http://10.0.2.2:8081/api/v1
```

Этот адрес работает в эмуляторе без дополнительных настроек!










