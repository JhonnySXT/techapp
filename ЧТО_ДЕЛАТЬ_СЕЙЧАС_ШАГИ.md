# 📋 Что нужно сделать СЕЙЧАС перед развертыванием

## ✅ Шаг 1: Создать иконки (5 минут)

**Вариант А - Быстро (рекомендуется):**
1. Откройте любой графический редактор (Paint, Photoshop, или онлайн)
2. Создайте два файла:
   - `icon-192.png` - размер 192x192 пикселей
   - `icon-512.png` - размер 512x512 пикселей
3. Можно просто зеленый квадрат с текстом "Дача" или эмодзи 🐻

**Вариант Б - Временно пропустить:**
- Можно развернуть без иконок, PWA будет работать, но без иконки на главном экране

## ✅ Шаг 2: Обновить Dockerfile (УЖЕ СДЕЛАНО ✅)

Dockerfile уже обновлен и будет копировать статические файлы.

## ✅ Шаг 3: Настроить переменные окружения на Render

После создания Web Service на Render:

1. Перейдите в **Environment** (Переменные окружения)
2. Добавьте:
   ```
   JWT_SECRET = <сгенерируйте ключ>
   PORT = 8081
   HOST = 0.0.0.0
   ```

**Как сгенерировать JWT_SECRET:**
- Windows PowerShell: `[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))`
- Или онлайн: https://www.random.org/strings/
- Минимум 32 символа, например: `MySuperSecretKeyForJWT12345678901234567890`

## ✅ Шаг 4: Загрузить код на GitHub

Убедитесь, что все файлы закоммичены:
- `test-app.html`
- `config.js`
- `manifest.json`
- `sw.js`
- `icon-192.png` (если создали)
- `icon-512.png` (если создали)
- `deploy/render/Dockerfile` (обновлен)

## ✅ Шаг 5: Развернуть на Render

Следуйте инструкциям в `deploy/render/ШАГИ_РАЗВЕРТЫВАНИЯ.md`

## ✅ Шаг 6: Протестировать

После развертывания:
1. Откройте URL сервера в браузере
2. Проверьте `/health` - должно вернуть "ok"
3. Откройте главную страницу - должна загрузиться форма входа
4. Протестируйте авторизацию

## ⚠️ Важно

Если иконки не созданы сейчас - это не критично! Можно развернуть без них и добавить позже.

---

**Текущий приоритет:**
1. 🔴 Создать иконки (или пропустить)
2. 🟡 Настроить переменные окружения на Render
3. 🟢 Развернуть на Render
4. 🟢 Протестировать

