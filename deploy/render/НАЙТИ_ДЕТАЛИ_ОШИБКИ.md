# 🔍 Найдена ошибка! Нужны детали

## ✅ Что нашли:

Вы нашли строку:
```
FAILURE: Build failed with an exception.
```

**Это начало ошибки!** Теперь нужно найти **детали**.

---

## 📋 Что искать дальше:

### Прокрутите лог ВВЕРХ или ВНИЗ от строки "FAILURE"

**Ищите строки, которые идут ПОСЛЕ "FAILURE":**

1. **"* What went wrong:"**
   ```
   * What went wrong:
   Task :server:shadowJar FAILED
   ...
   ```

2. **"* Try:"**
   ```
   * Try:
   Run with --info or --debug option to get more log output.
   ```

3. **Конкретная ошибка:**
   ```
   Execution failed for task ':server:shadowJar'.
   > ...
   ```

4. **Ошибки зависимостей:**
   ```
   Could not resolve all files for configuration ':server:runtimeClasspath'.
   > Could not find ...
   ```

5. **Ошибки компиляции:**
   ```
   e: file:///app/server/src/.../SomeFile.kt:XX:XX
   Unresolved reference: ...
   ```

---

## 📝 Что скопировать:

**Скопируйте блок ошибки:**

```
FAILURE: Build failed with an exception.

* What went wrong:
[ЗДЕСЬ БУДЕТ КОНКРЕТНАЯ ОШИБКА]

* Try:
[ЗДЕСЬ БУДУТ РЕКОМЕНДАЦИИ]
```

**Или если видите:**

```
> Task :server:shadowJar FAILED

FAILURE: Build failed with an exception.

* What went wrong:
Execution failed for task ':server:shadowJar'.
> [КОНКРЕТНАЯ ОШИБКА]
```

---

## 🎯 Где искать:

1. **Прокрутите ВНИЗ** от строки "FAILURE" (обычно детали идут сразу после)
2. **Ищите строки с "*"** (звездочкой) - это секции ошибки
3. **Ищите строки с ">"** (больше) - это вложенные ошибки

---

## 💡 Альтернатива:

Если не можете найти "What went wrong":
1. **Скопируйте 50-100 строк** вокруг строки "FAILURE"
2. **Отправьте мне** - я найду конкретную ошибку

---

**Прокрутите лог ВНИЗ от "FAILURE" и найдите строки с "* What went wrong:" - там будет конкретная ошибка!** 🔍










