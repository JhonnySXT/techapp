# Игнорируем ненужные файлы при сборке Docker образа для Render
.git
.gitignore
*.md
.idea
.vscode
*.iml
local.properties
*.log
.DS_Store
Thumbs.db

# Игнорируем другие модули (не нужны для сервера)
app/
calorie-app/
org-chat-agent/
scripts/
docs/
deploy/

# Игнорируем build артефакты (будем собирать заново)
server/build/
build/
.gradle

# Оставляем только то, что нужно для сборки сервера
!gradle/
!gradlew
!gradlew.bat
# НЕ копируем корневой build.gradle.kts (содержит Android-плагины)
# НЕ копируем settings.gradle.kts (создадим свой без Android)
# НЕ копируем gradle.properties (создадим свой)
!server/

