# 📖 Инструкция по запуску сервера Загородного клуба "Дача"

## Способ 1: Через Android Studio (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Откройте терминал в Android Studio

1. Откройте проект в Android Studio
2. Внизу экрана найдите вкладку **"Terminal"** (Терминал)
   - Если не видите, нажмите: `View` → `Tool Windows` → `Terminal`
   - Или используйте горячую клавишу: `Alt + F12`

### Шаг 2: Введите команду запуска

В терминале введите:
```
./gradlew :server:run
```

Или на Windows:
```
gradlew.bat :server:run
```

### Шаг 3: Дождитесь запуска

Вы увидите сообщения о запуске. Когда увидите:
```
Responding at http://127.0.0.1:8081
```
Сервер запущен! ✅

---

## Способ 2: Через двойной клик (САМЫЙ ПРОСТОЙ)

### Просто запустите файл:

**Дважды кликните на файл:** `start-server.bat`

Сервер запустится автоматически!

---

## Способ 3: Через командную строку Windows

### Шаг 1: Откройте командную строку

1. Нажмите `Win + R`
2. Введите: `cmd`
3. Нажмите Enter

### Шаг 2: Перейдите в папку проекта

Введите (замените путь на ваш):
```
cd C:\Users\Jhonny\AndroidStudioProjects\Techapp
```

### Шаг 3: Запустите сервер

Введите:
```
gradlew.bat :server:run
```

---

## ✅ Как проверить, что сервер работает

### Вариант 1: Через браузер

1. Откройте браузер (Chrome, Edge и т.д.)
2. Введите в адресной строке:
   ```
   http://localhost:8081/health
   ```
3. Должна появиться страница с текстом: `{"status":"ok"}`

### Вариант 2: Через Postman

1. Скачайте Postman: https://www.postman.com/downloads/
2. Установите и откройте
3. Создайте новый запрос:
   - Метод: `GET`
   - URL: `http://localhost:8081/health`
4. Нажмите "Send"
5. Должен вернуться ответ: `{"status":"ok"}`

---

## 🛑 Как остановить сервер

В терминале, где запущен сервер, нажмите:
```
Ctrl + C
```

---

## ❓ Частые проблемы

### Проблема: "Порт 8081 уже занят"

**Решение:**
1. Остановите предыдущий запуск сервера (Ctrl+C)
2. Или измените порт в файле `server/src/main/resources/application.conf`

### Проблема: "Gradle не найден"

**Решение:**
- Убедитесь, что вы в правильной папке проекта
- Попробуйте использовать `gradlew.bat` вместо `gradlew`

### Проблема: "Java не найдена"

**Решение:**
- Android Studio должна автоматически использовать свою Java
- Если не работает, проверьте настройки в `gradle.properties`

---

## 📝 Что дальше?

После запуска сервера:

1. **Протестируйте API через Postman:**
   - POST `http://localhost:8081/api/v1/auth/login`
   - Body:
     ```json
     {
       "email": "admin@techapp.local",
       "password": "admin123"
     }
     ```

2. **Или скачайте готовый тестовый скрипт:**
   - Запустите `test-api.ps1` (если есть PowerShell)

---

## 💡 Совет

**Самый простой способ для начала:**
1. Откройте Android Studio
2. Откройте терминал (Alt + F12)
3. Введите: `gradlew.bat :server:run`
4. Дождитесь сообщения "Responding at http://127.0.0.1:8081"
5. Готово! ✅



