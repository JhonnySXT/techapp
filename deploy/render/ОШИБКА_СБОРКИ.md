# 🔍 Ошибка сборки Gradle (exit code 1)

## ❌ Проблема:

Команда `./gradlew :server:shadowJar --no-daemon` завершилась с **exit code 1**.

Это означает **ошибку сборки Gradle**, а не проблему с правами.

---

## 🔍 Что нужно проверить в логах:

В логах Render найдите **красные строки** с ошибками. Обычно это:

### Возможные ошибки:

1. **"Task :server:shadowJar FAILED"**
   - Проблема с задачей shadowJar
   - Нужно проверить конфигурацию

2. **"Could not resolve..." или "Could not find..."**
   - Проблема с зависимостями
   - Нужно проверить репозитории

3. **"Compilation error" или "Unresolved reference"**
   - Ошибка компиляции Kotlin
   - Нужно проверить код

4. **"File not found" или "Cannot find"**
   - Не хватает файлов
   - Нужно добавить в Dockerfile

---

## ✅ Что я исправил:

1. **Добавил копирование resources:**
   - `COPY server/src/main/resources/ ./server/src/main/resources/`
   - Это нужно для `application.conf`

2. **Добавил --stacktrace:**
   - Теперь будут более подробные логи ошибок

---

## 🚀 Что делать дальше:

### Шаг 1: Загрузите исправление

```bash
cd /c/Users/Jhonny/AndroidStudioProjects/Techapp

git add deploy/render/Dockerfile
git commit -m "Fix: Add resources and stacktrace to Dockerfile"
git push
```

### Шаг 2: Проверьте логи нового деплоя

После нового деплоя:
1. Откройте логи
2. Найдите **красные строки** с ошибками
3. **Скопируйте текст ошибки** (особенно последние 20-30 строк)
4. **Отправьте мне** - помогу исправить!

---

## 📋 Что скопировать из логов:

**Скопируйте:**
- Все красные строки
- Строки с "FAILED", "ERROR", "Exception"
- Последние 20-30 строк логов
- Стек-трейс (если есть)

**Пример того, что нужно:**
```
> Task :server:shadowJar FAILED

FAILURE: Build failed with an exception.

* What went wrong:
...
* Try:
...
```

---

## 💡 Альтернативное решение:

Если проблема не решится, можно попробовать использовать **встроенный Gradle** из образа:

```dockerfile
RUN gradle :server:shadowJar --no-daemon --stacktrace
```

Вместо:
```dockerfile
RUN ./gradlew :server:shadowJar --no-daemon --stacktrace
```

Но сначала нужно увидеть конкретную ошибку в логах!

---

**Загрузите исправление и проверьте логи нового деплоя. Скопируйте текст ошибки и отправьте мне!** 🔍










