# 🔧 Исправление ошибки "Permission denied"

## ❌ Проблема:

В логах видна ошибка:
```
/bin/sh: ./gradlew: Permission denied
error: failed to solve: process "/bin/sh -c ./gradlew :server:shadowJar --no-daemon" did not complete successfully: exit code: 126
```

**Причина:** Файл `gradlew` не имеет прав на выполнение в Docker контейнере.

---

## ✅ Решение:

Я исправил Dockerfile - добавил команду для выдачи прав на выполнение:

```dockerfile
# Даем права на выполнение gradlew
RUN chmod +x ./gradlew
```

---

## 🚀 Что делать дальше:

### Шаг 1: Загрузите исправленный Dockerfile на GitHub

1. **В Git Bash выполните:**
```bash
cd /c/Users/Jhonny/AndroidStudioProjects/Techapp
git add deploy/render/Dockerfile
git commit -m "Fix: Add chmod for gradlew in Dockerfile"
git push
```

### Шаг 2: Render автоматически пересоберет

После `git push`:
- Render обнаружит изменения
- Автоматически запустит новую сборку
- Исправленный Dockerfile будет использован

### Шаг 3: Дождитесь успешного деплоя

- Подождите 8-12 минут
- Следите за логами в Render Dashboard
- Должна быть успешная сборка

---

## ⚠️ Важно:

Убедитесь, что в Render Dashboard указан правильный путь к Dockerfile:
- **Dockerfile Path:** `deploy/render/Dockerfile`

Если указан другой путь - исправьте в настройках сервиса!

---

## 🎯 После успешного деплоя:

1. Проверьте health endpoint:
   ```
   https://techapp-server.onrender.com/health
   ```

2. Получите URL для Android:
   ```
   https://techapp-server.onrender.com/api/v1
   ```

---

**Выполните `git push` и дождитесь новой сборки!** 🚀










