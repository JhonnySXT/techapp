# 🚀 Руководство по развертыванию сервера в интернете

## 📋 Обзор

Для работы Android приложения через интернет нужен сервер, доступный из интернета. Есть несколько вариантов хостинга - от бесплатных до платных.

---

## 💰 Варианты хостинга

### 🆓 Бесплатные варианты (для начала)

#### 1. **Render.com** (РЕКОМЕНДУЕТСЯ для начала)
- ✅ **Бесплатный tier:** 750 часов/месяц (достаточно для тестирования)
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Простая настройка
- ⚠️ Сервер "засыпает" после 15 минут бездействия (просыпается при запросе)
- 💰 **Цена:** Бесплатно (с ограничениями) или $7/месяц (без ограничений)

#### 2. **Railway.app**
- ✅ **Бесплатный tier:** $5 кредитов/месяц
- ✅ Автоматический деплой
- ✅ HTTPS
- ⚠️ Ограниченные ресурсы на бесплатном плане
- 💰 **Цена:** Бесплатно (с ограничениями) или от $5/месяц

#### 3. **Fly.io**
- ✅ **Бесплатный tier:** 3 shared-cpu VMs
- ✅ Хорошая производительность
- ✅ Глобальная сеть
- 💰 **Цена:** Бесплатно (с ограничениями) или от $1.94/месяц

#### 4. **Heroku** (ограниченный бесплатный план)
- ⚠️ Убрали бесплатный tier, но есть альтернативы

### 💵 Платные варианты (для продакшена)

#### 1. **VPS (Virtual Private Server)**
- **DigitalOcean:** от $4/месяц
- **Hetzner:** от €4.15/месяц (лучшее соотношение цена/качество)
- **Linode:** от $5/месяц
- **Vultr:** от $2.50/месяц

**Плюсы:**
- ✅ Полный контроль
- ✅ Нет ограничений
- ✅ Можно настроить как угодно

**Минусы:**
- ⚠️ Нужно самому настраивать
- ⚠️ Нужно следить за безопасностью

#### 2. **Управляемые сервисы**
- **AWS Elastic Beanstalk:** от $0 (платите за ресурсы)
- **Google Cloud Run:** от $0 (платите за использование)
- **Azure App Service:** от $13/месяц

---

## 🎯 Рекомендация для начала

**Начните с Render.com** - это самый простой вариант:
1. Бесплатно для тестирования
2. Автоматический деплой
3. HTTPS из коробки
4. Простая настройка

Потом, когда приложение будет работать, можно перейти на VPS для большей производительности.

---

## 📦 Подготовка к развертыванию

### Шаг 1: Создать JAR файл сервера

Сервер уже настроен для сборки. Нужно собрать JAR:

```bash
cd server
../gradlew shadowJar
```

Или через Android Studio:
1. Откройте терминал
2. Выполните: `gradlew.bat :server:shadowJar`
3. JAR файл будет в: `server/build/libs/server-all.jar`

### Шаг 2: Подготовить файлы для деплоя

Нужны файлы:
- `server-all.jar` - собранный сервер
- `application.conf` - конфигурация (опционально)
- `Dockerfile` (если используете Docker)
- Скрипт запуска

---

## 🚀 Вариант 1: Развертывание на Render.com

### Преимущества:
- ✅ Бесплатно для начала
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS автоматически
- ✅ Простая настройка

### Пошаговая инструкция:

#### 1. Подготовить проект

Создайте `Dockerfile` в корне проекта:

```dockerfile
FROM eclipse-temurin:17-jre-alpine

WORKDIR /app

# Копируем JAR файл
COPY server/build/libs/server-all.jar app.jar

# Создаем директорию для данных
RUN mkdir -p /app/data

# Открываем порт
EXPOSE 8081

# Запускаем сервер
ENTRYPOINT ["java", "-jar", "app.jar"]
```

#### 2. Зарегистрироваться на Render.com

1. Перейдите на https://render.com
2. Зарегистрируйтесь (можно через GitHub)
3. Подтвердите email

#### 3. Создать Web Service

1. Нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name:** `techapp-server`
   - **Environment:** `Docker`
   - **Region:** Выберите ближайший (например, Frankfurt)
   - **Branch:** `main` (или ваша ветка)
   - **Root Directory:** оставьте пустым
   - **Dockerfile Path:** `Dockerfile`
   - **Docker Build Context:** `.` (точка)
   - **Docker Command:** оставьте пустым (используется из Dockerfile)

#### 4. Настроить переменные окружения

В разделе "Environment Variables" добавьте:
- `PORT=8081` (или другой порт, который даст Render)
- `HOST=0.0.0.0`

#### 5. Деплой

1. Нажмите "Create Web Service"
2. Render автоматически:
   - Соберет Docker образ
   - Развернет сервер
   - Назначит URL (например: `https://techapp-server.onrender.com`)

#### 6. Получить URL

После деплоя вы получите URL вида:
```
https://techapp-server.onrender.com
```

**Ваш API URL будет:**
```
https://techapp-server.onrender.com/api/v1
```

⚠️ **Важно:** На бесплатном плане сервер "засыпает" после 15 минут бездействия. Первый запрос после пробуждения может занять 30-60 секунд.

---

## 🖥️ Вариант 2: Развертывание на VPS (Hetzner/DigitalOcean)

### Преимущества:
- ✅ Полный контроль
- ✅ Нет ограничений
- ✅ Лучшая производительность
- ✅ Дешевле в долгосрочной перспективе

### Пошаговая инструкция:

#### 1. Арендовать VPS

**Hetzner (рекомендую):**
1. Перейдите на https://www.hetzner.com/cloud
2. Зарегистрируйтесь
3. Создайте новый проект
4. Создайте сервер:
   - **Location:** Выберите ближайший (Nuremberg, Falkenstein)
   - **Image:** Ubuntu 22.04
   - **Type:** CX11 (€4.15/месяц) - достаточно для начала
   - **SSH Key:** Добавьте ваш SSH ключ
5. Нажмите "Create & Buy"

**DigitalOcean:**
1. Перейдите на https://www.digitalocean.com
2. Зарегистрируйтесь
3. Создайте Droplet:
   - **Image:** Ubuntu 22.04
   - **Plan:** Basic $4/месяц
   - **Region:** Выберите ближайший
   - **SSH Key:** Добавьте ваш SSH ключ

#### 2. Подключиться к серверу

```bash
ssh root@ваш-ip-адрес
```

#### 3. Установить Java 17

```bash
# Обновить систему
apt update && apt upgrade -y

# Установить Java 17
apt install -y openjdk-17-jre

# Проверить установку
java -version
```

#### 4. Создать пользователя для приложения

```bash
# Создать пользователя
adduser techapp
usermod -aG sudo techapp

# Переключиться на пользователя
su - techapp
```

#### 5. Загрузить JAR файл на сервер

**С вашего компьютера:**

```bash
# Собрать JAR
cd C:\Users\Jhonny\AndroidStudioProjects\Techapp
gradlew.bat :server:shadowJar

# Загрузить на сервер (через SCP или SFTP)
scp server/build/libs/server-all.jar techapp@ваш-ip:/home/techapp/
```

Или используйте FileZilla (SFTP клиент).

#### 6. Создать директорию для данных

```bash
mkdir -p /home/techapp/data
```

#### 7. Создать systemd service (для автозапуска)

```bash
sudo nano /etc/systemd/system/techapp.service
```

Содержимое файла:
```ini
[Unit]
Description=Techapp Server
After=network.target

[Service]
Type=simple
User=techapp
WorkingDirectory=/home/techapp
ExecStart=/usr/bin/java -jar /home/techapp/server-all.jar
Restart=always
RestartSec=10
Environment="PORT=8081"
Environment="HOST=0.0.0.0"

[Install]
WantedBy=multi-user.target
```

#### 8. Запустить сервис

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable techapp

# Запустить сервис
sudo systemctl start techapp

# Проверить статус
sudo systemctl status techapp

# Посмотреть логи
sudo journalctl -u techapp -f
```

#### 9. Настроить файрвол

```bash
# Разрешить порт 8081
sudo ufw allow 8081/tcp

# Включить файрвол
sudo ufw enable
```

#### 10. Настроить домен (опционально)

Если у вас есть домен:
1. Настройте A-запись, указывающую на IP вашего сервера
2. Установите Nginx как reverse proxy
3. Настройте SSL через Let's Encrypt

---

## 🔒 Настройка безопасности для продакшена

### 1. Изменить пароль администратора

После первого запуска на продакшене:
1. Войдите как админ
2. Измените пароль через API или напрямую в БД

### 2. Настроить HTTPS

**Для Render.com:** HTTPS включен автоматически

**Для VPS:** Используйте Nginx + Let's Encrypt

```bash
# Установить Nginx
sudo apt install nginx

# Установить Certbot
sudo apt install certbot python3-certbot-nginx

# Настроить SSL
sudo certbot --nginx -d ваш-домен.com
```

### 3. Настроить CORS правильно

В `server/src/main/kotlin/com/techapp/server/plugins/Security.kt`:

```kotlin
cors {
    allowHost("ваш-домен.com", schemes = listOf("https"))
    // Или для разработки:
    // allowHost("localhost:8080")
}
```

### 4. Изменить JWT_SECRET

В коде сервера должен быть секретный ключ для JWT. Убедитесь, что он сложный и не хранится в коде.

---

## 📱 Настройка Android приложения

После развертывания сервера, в Android приложении используйте:

### Для Render.com:
```kotlin
val API_BASE_URL = "https://techapp-server.onrender.com/api/v1"
```

### Для VPS с доменом:
```kotlin
val API_BASE_URL = "https://ваш-домен.com/api/v1"
```

### Для VPS без домена (по IP):
```kotlin
val API_BASE_URL = "http://ваш-ip-адрес:8081/api/v1"
```

⚠️ **Важно:** Для продакшена обязательно используйте HTTPS!

---

## 🧪 Тестирование после развертывания

1. **Проверить health endpoint:**
   ```
   https://ваш-сервер.com/health
   ```
   Должен вернуть: `{"status":"ok"}`

2. **Проверить авторизацию:**
   ```bash
   curl -X POST https://ваш-сервер.com/api/v1/auth/login \
     -H "Content-Type: application/json" \
     -d '{"email":"admin@techapp.local","password":"admin123"}'
   ```

3. **Протестировать из Android приложения:**
   - Установите приложение
   - Попробуйте войти
   - Проверьте все функции

---

## 💡 Рекомендации

### Для начала (тестирование):
- ✅ Используйте **Render.com** (бесплатно, просто)

### Для продакшена (реальные пользователи):
- ✅ Используйте **VPS (Hetzner)** - лучшее соотношение цена/качество
- ✅ Настройте домен и HTTPS
- ✅ Настройте резервное копирование БД

### Масштабирование:
- Если пользователей станет много, можно:
  - Увеличить ресурсы VPS
  - Использовать несколько серверов с балансировщиком
  - Перейти на PostgreSQL вместо SQLite

---

## ❓ Частые вопросы

**Q: Сколько стоит хостинг?**
A: От бесплатно (Render.com для тестирования) до €4-10/месяц (VPS для продакшена)

**Q: Нужен ли домен?**
A: Не обязательно, но рекомендуется для продакшена. Можно использовать IP адрес.

**Q: Как часто нужно делать резервные копии?**
A: Рекомендуется ежедневно. SQLite файл можно копировать автоматически.

**Q: Что делать если сервер упал?**
A: На VPS с systemd сервис автоматически перезапустится. На Render.com нужно проверить логи.

---

## 🎯 План действий

1. **Выберите хостинг:**
   - Для начала: Render.com
   - Для продакшена: VPS (Hetzner)

2. **Подготовьте проект:**
   - Соберите JAR файл
   - Создайте Dockerfile (для Render)

3. **Разверните сервер:**
   - Следуйте инструкциям выше

4. **Протестируйте:**
   - Проверьте все endpoints
   - Протестируйте из Android приложения

5. **Настройте безопасность:**
   - Измените пароль админа
   - Настройте HTTPS
   - Настройте CORS

6. **Обновите Android приложение:**
   - Измените URL на продакшен
   - Протестируйте все функции

---

**Готов помочь с развертыванием!** Сообщите, какой вариант вы выбрали, и я помогу с конкретными шагами.

