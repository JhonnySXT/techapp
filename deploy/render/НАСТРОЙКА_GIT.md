# ⚙️ Настройка Git (имя и email)

## ❌ Проблема
Git требует указать ваше имя и email перед первым коммитом.

## ✅ Решение

Выполните в Git Bash эти две команды (замените на ваши данные):

### 1. Настройте email (используйте ваш GitHub email):
```bash
git config --global user.email "ваш-email@example.com"
```

### 2. Настройте имя (любое имя, например ваше имя):
```bash
git config --global user.name "Ваше Имя"
```

**Пример:**
```bash
git config --global user.email "jhonny@example.com"
git config --global user.name "Jhonny"
```

---

## 🔄 После настройки повторите коммит:

```bash
git commit -m "Initial commit: Techapp server ready for deployment"
```

---

## ✅ Проверка настроек:

После настройки можете проверить:
```bash
git config --global user.email
git config --global user.name
```

---

**Важно:** Используйте email, который привязан к вашему GitHub аккаунту (или тот, который будете использовать).

**Выполните эти команды и повторите коммит!** 🚀










