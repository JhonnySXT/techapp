# 🚀 Развертывание на Render.com

Эта папка содержит все необходимое для развертывания сервера Techapp на Render.com.

## 📋 Что здесь находится

- **Dockerfile** - файл для сборки Docker образа
- **.dockerignore** - файлы, которые не нужно копировать в образ
- **README.md** - эта инструкция
- **render.yaml** (опционально) - конфигурация для автоматического создания сервиса

## 🎯 Быстрый старт

### Шаг 1: Загрузите проект на GitHub

Если проект еще не на GitHub:

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/techapp.git
git push -u origin main
```

### Шаг 2: Зарегистрируйтесь на Render.com

1. Перейдите на https://render.com
2. Нажмите "Get Started for Free"
3. Зарегистрируйтесь через GitHub (самый простой способ)
4. Подтвердите email

### Шаг 3: Создайте Web Service

1. В панели Render нажмите **"New +"** → **"Web Service"**

2. Подключите GitHub репозиторий:
   - Нажмите "Connect account" если нужно
   - Выберите ваш репозиторий `techapp`

3. Заполните настройки:
   - **Name:** `techapp-server` (или любое имя)
   - **Environment:** `Docker`
   - **Region:** Выберите ближайший (например, `Frankfurt (EU)`)
   - **Branch:** `main` (или ваша ветка)
   - **Root Directory:** оставьте пустым
   - **Dockerfile Path:** `deploy/render/Dockerfile`
   - **Docker Build Context:** `.` (точка - корень проекта)
   - **Docker Command:** оставьте пустым

4. Нажмите **"Advanced"** и добавьте переменные окружения:
   - **Key:** `PORT`, **Value:** `8081` (или оставьте пустым, Render назначит автоматически)
   - **Key:** `HOST`, **Value:** `0.0.0.0`

5. Нажмите **"Create Web Service"**

### Шаг 4: Дождаться деплоя

Render автоматически:
- ✅ Соберет Docker образ
- ✅ Развернет сервер
- ✅ Назначит URL

Процесс займет 5-10 минут. Вы увидите логи сборки в реальном времени.

### Шаг 5: Получить URL

После успешного деплоя вы получите URL вида:
```
https://techapp-server.onrender.com
```

**Ваш API URL будет:**
```
https://techapp-server.onrender.com/api/v1
```

## 🧪 Тестирование

### 1. Проверить health endpoint

Откройте в браузере:
```
https://techapp-server.onrender.com/health
```

Должно показать: `{"status":"ok"}`

### 2. Проверить авторизацию

Используйте Postman или curl:
```bash
curl -X POST https://techapp-server.onrender.com/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@techapp.local","password":"admin123"}'
```

Должен вернуться токен.

## 📱 Настройка Android приложения

В Android приложении используйте URL:

```kotlin
val API_BASE_URL = "https://techapp-server.onrender.com/api/v1"
```

Или через BuildConfig:
```kotlin
buildConfigField("String", "API_BASE_URL", "\"https://techapp-server.onrender.com/api/v1\"")
```

## ⚠️ Важные замечания

### О бесплатном плане Render.com:

1. **Сервер "засыпает"** после 15 минут бездействия
2. **Первый запрос** после пробуждения может занять 30-60 секунд
3. **Для продакшена** лучше использовать платный план ($7/месяц) или VPS

### Если сервер не запускается:

1. Проверьте логи в Render Dashboard
2. Убедитесь, что Dockerfile находится по пути `deploy/render/Dockerfile`
3. Проверьте переменные окружения
4. Убедитесь, что порт настроен правильно

## 🔄 Обновление сервера

После изменений в коде:

1. Загрузите изменения на GitHub:
   ```bash
   git add .
   git commit -m "Update server"
   git push
   ```

2. Render автоматически:
   - Определит изменения
   - Пересоберет Docker образ
   - Развернет новую версию

Или можно запустить деплой вручную из панели Render.

## 🔒 Безопасность

После первого развертывания:

1. **Измените пароль администратора** через API или напрямую в БД
2. **Настройте CORS** правильно (если нужно ограничить доступ)
3. **Используйте HTTPS** (Render предоставляет автоматически)

## 💰 Стоимость

- **Бесплатный план:** 0₽/месяц (с ограничениями)
- **Платный план:** $7/месяц (без ограничений)

Для начала бесплатного плана достаточно!

## 📚 Дополнительная документация

- Полное руководство: `../../DEPLOYMENT_GUIDE.md`
- Быстрая инструкция: `../../QUICK_DEPLOY.md`
- Настройка URL в Android: `../../ANDROID_URL_CONFIG.md`

## ❓ Проблемы?

Если что-то не работает:

1. Проверьте логи в Render Dashboard
2. Убедитесь, что сервер запущен (зеленый индикатор)
3. Проверьте health endpoint
4. Убедитесь, что Dockerfile путь правильный: `deploy/render/Dockerfile`

## 🎉 Готово!

Теперь ваш сервер доступен из интернета, и Android приложение может работать откуда угодно!










