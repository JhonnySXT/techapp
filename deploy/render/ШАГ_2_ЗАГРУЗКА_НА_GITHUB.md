# 📤 Шаг 2: Загрузка проекта на GitHub

## ⚠️ Важно: Перезапустите терминал!

После установки Git нужно **перезапустить терминал** или **Android Studio**, чтобы Git стал доступен.

---

## ✅ Проверка установки Git

После перезапуска терминала выполните:

```bash
git --version
```

Должно показать что-то вроде: `git version 2.xx.x`

**Если Git все еще не найден:**
1. Закройте Android Studio полностью
2. Откройте заново
3. Откройте терминал (Alt + F12)
4. Попробуйте снова: `git --version`

---

## 🚀 Пошаговая загрузка на GitHub

### Шаг 1: Инициализация репозитория

В терминале Android Studio (Alt + F12) выполните:

```bash
git init
```

### Шаг 2: Добавление всех файлов

```bash
git add .
```

### Шаг 3: Создание первого коммита

```bash
git commit -m "Initial commit: Techapp server ready for deployment"
```

### Шаг 4: Создание репозитория на GitHub

1. **Откройте:** https://github.com/new

2. **Заполните:**
   - **Repository name:** `techapp` (или любое другое имя)
   - **Description:** `Techapp - Загородный клуб "Дача" - Server for Android app`
   - **Visibility:** Public или Private (на ваше усмотрение)
   - ⚠️ **НЕ добавляйте** README, .gitignore или лицензию (они уже есть в проекте!)

3. **Нажмите:** "Create repository"

### Шаг 5: Подключение и загрузка

GitHub покажет инструкции. Выполните (замените `ваш-username` на ваш GitHub username):

```bash
git remote add origin https://github.com/ваш-username/techapp.git
git branch -M main
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub.

---

## 🔐 Авторизация в Git

При первом push Git может попросить авторизацию:

### Вариант 1: Personal Access Token (рекомендуется)

1. Перейдите: https://github.com/settings/tokens
2. Нажмите "Generate new token (classic)"
3. Название: `Techapp deployment`
4. Выберите срок действия (например, 90 дней)
5. Отметьте scope: `repo` (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **Скопируйте токен** (он показывается только один раз!)
8. Используйте токен вместо пароля при push

### Вариант 2: GitHub Desktop

Если возникают проблемы с авторизацией, используйте GitHub Desktop:
1. Скачайте: https://desktop.github.com/
2. Войдите в аккаунт
3. File → Add Local Repository
4. Выберите папку проекта
5. Publish repository

---

## ✅ Проверка

После успешной загрузки:

1. Откройте ваш репозиторий на GitHub
2. Должны быть видны все файлы проекта
3. Особенно проверьте, что есть папка `deploy/render/` с Dockerfile

---

## 🎯 Что дальше?

После загрузки проекта на GitHub:
1. ✅ Регистрация на Render.com
2. ✅ Создание Web Service
3. ✅ Развертывание сервера

---

## ❓ Проблемы?

**Проблема:** "git: command not found"
- **Решение:** Перезапустите терминал/Android Studio

**Проблема:** "Permission denied"
- **Решение:** Используйте Personal Access Token вместо пароля

**Проблема:** "Repository not found"
- **Решение:** Проверьте, что репозиторий создан на GitHub и имя правильное

---

**Готовы продолжить?** После загрузки на GitHub сообщите, и перейдем к Render.com! 🚀

