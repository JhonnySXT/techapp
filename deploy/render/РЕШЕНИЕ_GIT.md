# 🔧 Решение проблемы с Git

## Проблема
Git не найден в PowerShell терминале Android Studio, но видно, что открыт Git Bash.

## ✅ Решение 1: Использовать Git Bash (самый простой!)

На скриншоте видно, что у вас открыт **Git Bash** (MINGW64). Git там точно работает!

### Что делать:

1. **В Git Bash терминале** выполните команды:

```bash
# Перейдите в папку проекта
cd /c/Users/Jhonny/AndroidStudioProjects/Techapp

# Проверьте Git
git --version

# Инициализируйте репозиторий
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Techapp server ready for deployment"
```

2. **Создайте репозиторий на GitHub:**
   - Откройте: https://github.com/new
   - Название: `techapp`
   - НЕ добавляйте README, .gitignore, лицензию
   - Нажмите "Create repository"

3. **В Git Bash выполните** (замените `ваш-username`):
```bash
git remote add origin https://github.com/ваш-username/techapp.git
git branch -M main
git push -u origin main
```

---

## ✅ Решение 2: Использовать GitHub Desktop (еще проще!)

Если не хотите работать с командной строкой:

1. **Скачайте GitHub Desktop:**
   - https://desktop.github.com/
   - Установите
   - Войдите в аккаунт GitHub

2. **В GitHub Desktop:**
   - File → Add Local Repository
   - Выберите папку: `C:\Users\Jhonny\AndroidStudioProjects\Techapp`
   - Нажмите "Add repository"

3. **Опубликуйте:**
   - Нажмите "Publish repository"
   - Название: `techapp`
   - Public или Private
   - Нажмите "Publish repository"

**Готово!** Проект на GitHub!

---

## ✅ Решение 3: Добавить Git в PATH (для PowerShell)

Если хотите, чтобы Git работал в PowerShell:

1. **Найдите, где установлен Git:**
   - Обычно: `C:\Program Files\Git\cmd\`
   - Или: `C:\Program Files (x86)\Git\cmd\`

2. **Добавьте в PATH:**
   - Win + R → `sysdm.cpl` → Enter
   - Вкладка "Дополнительно" → "Переменные среды"
   - В "Системные переменные" найдите `Path`
   - Нажмите "Изменить"
   - "Создать" → вставьте путь к Git (например: `C:\Program Files\Git\cmd`)
   - "ОК" везде
   - Перезапустите Android Studio

---

## 🎯 Рекомендация

**Используйте Git Bash** - он уже открыт и там Git точно работает!

Или **GitHub Desktop** - самый простой вариант без командной строки.

---

**Какой вариант выбираете?** Сообщите, и продолжим! 🚀

