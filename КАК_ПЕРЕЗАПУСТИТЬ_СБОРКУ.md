# 🔄 Как перезапустить сборку на Render.com

## 🚀 Способ 1: Через веб-интерфейс Render (РЕКОМЕНДУЕТСЯ)

### Шаги:

1. **Откройте Render Dashboard:**
   - Перейдите на https://dashboard.render.com
   - Войдите в свой аккаунт

2. **Найдите ваш сервис:**
   - В списке сервисов найдите `techapp-server` (или как вы его назвали)
   - Нажмите на него

3. **Запустите ручной деплой:**
   - В правом верхнем углу найдите кнопку **"Manual Deploy"**
   - Нажмите на неё
   - Выберите **"Deploy latest commit"** (если вы сделали commit изменений)
   - Или **"Deploy specific commit"** (если хотите выбрать конкретный коммит)

4. **Дождитесь сборки:**
   - Render начнет сборку автоматически
   - Вы увидите логи в реальном времени
   - Обычно это занимает 5-10 минут

---

## 🔄 Способ 2: Через Git Push (автоматический)

Если у вас настроен **Auto-Deploy**:

1. **Сделайте commit изменений:**
   ```bash
   git add .
   git commit -m "Fix: исправлена сборка Dockerfile"
   git push origin main
   ```

2. **Render автоматически запустит сборку:**
   - После push в GitHub
   - Render обнаружит изменения
   - Начнет автоматическую сборку

---

## 📋 Способ 3: Через кнопку "Retry" (если была ошибка)

Если сборка упала с ошибкой:

1. Откройте ваш сервис в Render Dashboard
2. Перейдите на вкладку **"Events"** или **"Logs"**
3. Найдите последний неудачный деплой
4. Нажмите кнопку **"Retry"** рядом с ним

---

## 🔍 Как проверить статус сборки

1. **В Dashboard:**
   - Статус отображается рядом с названием сервиса
   - 🟢 **Live** - работает
   - 🟡 **Building** - идет сборка
   - 🔴 **Build Failed** - ошибка сборки

2. **В логах:**
   - Нажмите на сервис
   - Перейдите на вкладку **"Logs"**
   - Вы увидите все логи сборки в реальном времени

---

## ⚡ Быстрая команда (если используете Git)

```bash
# 1. Добавить все изменения
git add .

# 2. Сделать commit
git commit -m "Fix: исправлена конфигурация сборки"

# 3. Отправить на GitHub
git push origin main

# Render автоматически начнет сборку!
```

---

## ❓ Что делать, если сборка все еще падает?

1. **Проверьте логи:**
   - Откройте вкладку **"Logs"** в Render Dashboard
   - Найдите строку с `ERROR` или `BUILD FAILED`
   - Скопируйте ошибку

2. **Проверьте настройки:**
   - **Dockerfile Path:** должен быть `deploy/render/Dockerfile`
   - **Docker Build Context:** должен быть `.` (точка)
   - **Branch:** должен быть `main` (или ваша ветка)

3. **Проверьте файлы:**
   - Убедитесь, что все файлы закоммичены в Git
   - Проверьте, что `deploy/render/Dockerfile` существует

---

## 📝 Чеклист перед перезапуском

- [ ] Все изменения сохранены в файлах
- [ ] Изменения закоммичены в Git (если используете auto-deploy)
- [ ] Dockerfile находится в `deploy/render/Dockerfile`
- [ ] Переменные окружения настроены (PORT, HOST)

---

## 🎯 После успешной сборки

1. **Проверьте URL:**
   - Ваш сервер будет доступен по адресу: `https://ваш-сервис.onrender.com`
   - API: `https://ваш-сервис.onrender.com/api/v1`

2. **Проверьте работоспособность:**
   - Откройте: `https://ваш-сервис.onrender.com/health`
   - Должен вернуться: `{"status":"ok"}`

3. **Обновите config.js:**
   - Если нужно, обновите `API_BASE` в `config.js` на новый URL

---

**Удачи! 🚀**

