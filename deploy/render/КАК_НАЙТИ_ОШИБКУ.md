# 🔍 Как найти конкретную ошибку в логах

## 📋 Инструкция:

### Шаг 1: Прокрутите логи ВВЕРХ

В Render Dashboard в логах **прокрутите вверх** от красной строки с ошибкой.

### Шаг 2: Ищите эти строки:

**Обычно ошибка Gradle выглядит так:**

```
FAILURE: Build failed with an exception.

* What went wrong:
Task :server:shadowJar FAILED
...
* Try:
Run with --info or --debug option to get more log output.
```

**Или:**

```
> Task :server:shadowJar FAILED

FAILURE: Build failed with an exception.

* What went wrong:
Execution failed for task ':server:shadowJar'.
> ...
```

**Или ошибки зависимостей:**

```
Could not resolve all files for configuration ':server:runtimeClasspath'.
> Could not find io.ktor:ktor-server-core-jvm:2.3.7.
```

---

## 📝 Что скопировать:

**Скопируйте:**
1. **Все строки с "FAILURE"**
2. **Секцию "What went wrong"** (если есть)
3. **Строки с "Task :server:..." FAILED**
4. **Строки с "Could not resolve"** или "Could not find"
5. **Последние 50-100 строк** перед Java stack trace

---

## 🎯 Где искать:

**В логах Render:**
1. Прокрутите **ВВЕРХ** от красной строки `ERROR: process...`
2. Ищите строки **ПЕРЕД** Java stack trace
3. Обычно ошибка находится **за 20-50 строк** до stack trace

---

## 💡 Альтернатива:

Если не можете найти конкретную ошибку:
1. **Скопируйте последние 100-200 строк** логов
2. **Отправьте мне** - я найду ошибку

---

**Прокрутите логи ВВЕРХ и найдите строки с "FAILURE" или "What went wrong" - это конкретная ошибка!** 🔍










