# 📋 Краткая инструкция по развертыванию на Render.com

## ✅ Что уже готово

Все файлы для развертывания находятся в этой папке:
- ✅ `Dockerfile` - собирает сервер из исходников
- ✅ `.dockerignore` - исключает ненужные файлы
- ✅ `README.md` - подробная инструкция
- ✅ `render.yaml` - конфигурация (опционально)

## 🚀 Быстрые шаги

### 1. Загрузите проект на GitHub (если еще не загружен)

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/techapp.git
git push -u origin main
```

### 2. Зарегистрируйтесь на Render.com

1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub
3. Подтвердите email

### 3. Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name:** `techapp-server`
   - **Environment:** `Docker`
   - **Region:** `Frankfurt (EU)` (или ближайший)
   - **Branch:** `main`
   - **Dockerfile Path:** `deploy/render/Dockerfile` ⚠️ **ВАЖНО!**
   - **Docker Build Context:** `.` (точка)
4. Переменные окружения:
   - `PORT=8081`
   - `HOST=0.0.0.0`
5. Нажмите **"Create Web Service"**

### 4. Дождитесь деплоя (5-10 минут)

Render автоматически соберет и развернет сервер.

### 5. Получите URL

После деплоя получите URL вида:
```
https://techapp-server.onrender.com/api/v1
```

## 📱 Использование в Android приложении

```kotlin
val API_BASE_URL = "https://techapp-server.onrender.com/api/v1"
```

## ⚠️ Важно

- **Dockerfile Path:** обязательно укажите `deploy/render/Dockerfile`
- **Docker Build Context:** должен быть `.` (корень проекта)
- На бесплатном плане сервер "засыпает" после 15 минут

## 📚 Подробная инструкция

См. `README.md` в этой же папке.

## ❓ Проблемы?

1. Проверьте логи в Render Dashboard
2. Убедитесь, что путь к Dockerfile правильный
3. Проверьте переменные окружения

