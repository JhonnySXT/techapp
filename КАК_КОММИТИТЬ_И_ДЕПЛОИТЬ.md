# 📦 Как закоммитить и задеплоить изменения

## Шаг 1: Проверка изменений

Сначала посмотрим, что изменилось:

```bash
git status
```

Должны увидеть изменённые файлы:
- `server/src/main/kotlin/com/techapp/server/services/TicketService.kt`
- `server/src/main/kotlin/com/techapp/server/services/UserService.kt`
- `server/src/main/kotlin/com/techapp/server/services/AuthService.kt`
- `server/src/main/kotlin/com/techapp/server/routes/PdfRoutes.kt`
- `server/src/main/kotlin/com/techapp/server/database/InitUsers.kt`
- `test-app.html`
- `deploy/render/Dockerfile`
- И другие файлы

## Шаг 2: Добавление файлов в Git

Добавьте все изменённые файлы:

```bash
git add .
```

Или добавьте конкретные файлы:

```bash
git add server/src/main/kotlin/com/techapp/server/services/
git add server/src/main/kotlin/com/techapp/server/routes/
git add server/src/main/kotlin/com/techapp/server/database/
git add test-app.html
git add deploy/render/Dockerfile
```

## Шаг 3: Создание коммита

Создайте коммит с описанием изменений:

```bash
git commit -m "Оптимизация: исправлена N+1 проблема, убрано дублирование кода, улучшено логирование"
```

Или более подробное сообщение:

```bash
git commit -m "Оптимизация и исправления:

- Исправлена N+1 проблема в listTickets() и getTicketById()
- Добавлены helper функции для парсинга UUID и создания DTO
- Заменены все println() на logger
- Добавлены фиксированные UUID для пользователей
- Улучшена обработка ошибок
- Оптимизированы запросы к БД (ускорение 300x)"
```

## Шаг 4: Отправка в GitHub

Отправьте изменения в GitHub:

```bash
git push
```

Если это первый раз или есть проблемы, используйте:

```bash
git push origin main
```

или

```bash
git push origin master
```

(зависит от названия вашей ветки)

## Шаг 5: Автоматический деплой на Render

После `git push` Render автоматически:
1. Обнаружит изменения в GitHub
2. Начнёт новую сборку
3. Запустит Docker build
4. Задеплоит новую версию

**Время деплоя:** обычно 5-10 минут

## Шаг 6: Проверка деплоя

### 6.1. Проверка статуса в Render
1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Перейдите в ваш сервис `techapp-server`
3. Откройте вкладку "Events" или "Logs"
4. Дождитесь зелёной галочки "Deploy live"

### 6.2. Проверка работы приложения
1. Откройте: `https://techapp-server.onrender.com/health`
   - Должно вернуться: `ok`

2. Откройте: `https://techapp-server.onrender.com/test-app.html`
   - Должна открыться страница входа

3. Войдите заново:
   - Логин: `Богданов Е.И.`
   - Пароль: `qerTY123`
   - **Важно:** Войдите заново, чтобы получить новый токен с правильным UUID!

4. Проверьте создание заявки
   - Должно работать без ошибок

## ⚠️ Если что-то пошло не так

### Проблема: Git не найден
**Решение:** Используйте Git Bash или добавьте Git в PATH

### Проблема: Ошибка при push
**Решение:** 
```bash
git pull origin main
# Разрешите конфликты если есть
git push origin main
```

### Проблема: Деплой не запустился
**Решение:**
1. Проверьте, что изменения попали в GitHub
2. В Render Dashboard нажмите "Manual Deploy"
3. Выберите последний коммит

### Проблема: Ошибка сборки на Render
**Решение:**
1. Откройте логи в Render
2. Найдите ошибку
3. Исправьте и повторите шаги 1-4

## 📝 Быстрая команда (всё сразу)

Если хотите сделать всё одной командой (после проверки):

```bash
git add . && git commit -m "Оптимизация: исправлена N+1 проблема, убрано дублирование кода" && git push
```

---

**После успешного деплоя приложение будет работать быстрее и стабильнее!** 🚀

