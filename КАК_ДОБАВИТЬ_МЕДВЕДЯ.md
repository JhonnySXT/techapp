# 🐻 Как добавить своё изображение медведя

## Вариант 1: Использовать своё изображение медведя

1. Найдите или скачайте изображение медведя (формат: JPG, PNG, SVG)
2. Поместите файл в папку проекта (например, `bear.jpg` или `bear.png`)
3. Откройте файл `test-app.html` в текстовом редакторе
4. Найдите строку с `body::after` (примерно строка 49)
5. Замените `background-image: url('data:image/svg+xml,...')` на:

```css
background-image: url('bear.jpg');  /* или bear.png, или путь к вашему файлу */
```

6. Сохраните файл и обновите страницу в браузере

## Вариант 2: Использовать изображение из интернета

1. Найдите изображение медведя в интернете
2. Скопируйте ссылку на изображение (URL)
3. В файле `test-app.html` замените `background-image` на:

```css
background-image: url('https://ваша-ссылка-на-изображение.jpg');
```

## Пример готового кода:

```css
body::after {
    content: '';
    position: fixed;
    bottom: 10px;
    right: 10px;
    width: 280px;
    height: 280px;
    z-index: 0;
    pointer-events: none;
    background-image: url('bear.jpg');  /* Ваше изображение */
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    opacity: 0.6;
    animation: bearEatHoney 4s ease-in-out infinite;
    filter: drop-shadow(0 10px 20px rgba(0,0,0,0.6));
}
```

## Рекомендации:

- **Размер изображения**: лучше использовать изображение размером 500x500px или больше
- **Формат**: JPG или PNG подойдут лучше всего
- **Прозрачность**: если хотите, чтобы фон был прозрачным, используйте PNG с прозрачностью
- **Позиция**: медведь будет в правом нижнем углу страницы

## Если хотите, чтобы медведь был больше/меньше:

Измените значения `width` и `height` в CSS (сейчас 280px):

```css
width: 350px;  /* больше */
height: 350px; /* больше */
```

или

```css
width: 200px;  /* меньше */
height: 200px; /* меньше */
```

