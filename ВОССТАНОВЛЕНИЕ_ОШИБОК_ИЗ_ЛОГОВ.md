# 🔍 Восстановление ошибок из логов Git Bash и Render

## 📋 История ошибок при деплое

### ❌ Ошибка 1: Permission denied для gradlew

**Ошибка в логах:**
```
error: failed to solve: process "/bin/sh -c ./gradlew :server:shadowJar --no-daemon" did not complete successfully: exit code: 126
./gradlew: Permission denied
```

**Причина:**
- Файл `gradlew` не имел прав на выполнение в Docker контейнере

**Решение:**
Добавлено в Dockerfile:
```dockerfile
RUN chmod +x ./gradlew
```

**Команды Git Bash для исправления:**
```bash
cd /c/Users/Jhonny/AndroidStudioProjects/Techapp
git add deploy/render/Dockerfile
git commit -m "Fix: Add chmod for gradlew in Dockerfile"
git push
```

---

### ❌ Ошибка 2: Ошибка сборки Gradle (exit code 1)

**Ошибка в логах:**
```
> Task :server:shadowJar FAILED

FAILURE: Build failed with an exception.

* What went wrong:
Execution failed for task ':server:shadowJar'.
```

**Возможные причины:**
1. Отсутствие файла `application.conf` (resources)
2. Ошибки компиляции Kotlin
3. Проблемы с зависимостями

**Решение 1: Добавление resources**
```dockerfile
COPY server/src/main/resources/ ./server/src/main/resources/
```

**Решение 2: Добавление --stacktrace для детальных логов**
```dockerfile
RUN ./gradlew :server:shadowJar --no-daemon --stacktrace
```

**Команды Git Bash:**
```bash
cd /c/Users/Jhonny/AndroidStudioProjects/Techapp
git add deploy/render/Dockerfile
git commit -m "Fix: Add resources and stacktrace to Dockerfile"
git push
```

---

### ❌ Ошибка 3: Ошибки компиляции Kotlin

**Ошибка в логах:**
```
[ERROR] e: file:///app/server/src/.../SomeFile.kt:XX:XX
Unresolved reference: ...
Compilation error
```

**Что проверялось:**
- Использование `epochSecond` (может быть проблема с версией Kotlin)
- Импорты `kotlinx.serialization`
- Синтаксические ошибки

**Как получить детальные логи:**
1. В Render Dashboard → Logs
2. Найдите строки с `[ERROR]` или `Compilation error`
3. Скопируйте все строки, начиная с `> Task :server:compileKotlin FAILED`

---

### ❌ Ошибка 4: Проблемы с зависимостями

**Ошибка в логах:**
```
Could not resolve all files for configuration ':server:runtimeClasspath'.
> Could not find io.ktor:ktor-server-core-jvm:2.3.7.
```

**Решение:**
- Проверка репозиториев в `build.gradle.kts`
- Добавление необходимых репозиториев (Maven Central, etc.)

---

### ❌ Ошибка 5: Использование встроенного Gradle

**Проблема:**
- `gradlew` продолжал падать с ошибками

**Альтернативное решение:**
Использовать встроенный Gradle из образа `gradle:8.13-jdk17-alpine`:
```dockerfile
RUN gradle :server:shadowJar --no-daemon --stacktrace
```

Вместо:
```dockerfile
RUN ./gradlew :server:shadowJar --no-daemon --stacktrace
```

---

## 📝 Типичные команды Git Bash для исправления

### Базовая последовательность:

```bash
# 1. Переход в папку проекта
cd /c/Users/Jhonny/AndroidStudioProjects/Techapp

# 2. Проверка статуса
git status

# 3. Добавление изменений
git add deploy/render/Dockerfile

# 4. Коммит
git commit -m "Fix: Описание исправления"

# 5. Отправка на GitHub
git push
```

### Проверка изменений на GitHub:

```bash
# Проверить, что изменения отправлены
git log --oneline -5
```

---

## 🔍 Как найти конкретную ошибку в логах Render

### Шаг 1: Откройте логи
1. Render Dashboard → Ваш сервис → **Logs**
2. Или кликните на неудачный деплой → откроются логи

### Шаг 2: Ищите эти строки

**Ошибки Gradle:**
```
FAILURE: Build failed with an exception.

* What went wrong:
Task :server:shadowJar FAILED
...
* Try:
Run with --info or --debug option to get more log output.
```

**Ошибки компиляции:**
```
> Task :server:compileKotlin FAILED
[ERROR] e: file:///app/server/src/.../SomeFile.kt:XX:XX
Unresolved reference: ...
```

**Ошибки зависимостей:**
```
Could not resolve all files for configuration ':server:runtimeClasspath'.
> Could not find io.ktor:ktor-server-core-jvm:2.3.7.
```

### Шаг 3: Прокрутите ВВЕРХ или ВНИЗ

- **ВВЕРХ** - ищите начало ошибки (обычно "FAILURE" или "ERROR")
- **ВНИЗ** - ищите детали после "FAILURE" (секция "What went wrong")

### Шаг 4: Скопируйте

**Что копировать:**
- Все строки с "FAILURE" или "ERROR"
- Секцию "What went wrong" (если есть)
- Последние 30-50 строк перед Java stack trace
- Все красные строки

---

## 🛠️ Текущий Dockerfile (после исправлений)

Основные исправления, которые были применены:

1. **chmod для gradlew:**
   ```dockerfile
   RUN chmod +x ./gradlew
   ```

2. **Копирование resources:**
   ```dockerfile
   COPY server/src/main/resources/ ./server/src/main/resources/
   ```

3. **Добавление --stacktrace:**
   ```dockerfile
   RUN ./gradlew :server:shadowJar --no-daemon --stacktrace
   ```

4. **Альтернатива - встроенный Gradle:**
   ```dockerfile
   RUN gradle :server:shadowJar --no-daemon --stacktrace
   ```

---

## 📊 Статус исправлений

### ✅ Исправлено:
- [x] Permission denied для gradlew
- [x] Добавление resources в Dockerfile
- [x] Детальное логирование (--stacktrace)
- [x] Альтернативный вариант с встроенным Gradle

### ⚠️ Требует проверки:
- [ ] Ошибки компиляции Kotlin (если были)
- [ ] Проблемы с зависимостями (если были)
- [ ] Другие специфичные ошибки из логов

---

## 🚀 Что делать, если ошибка повторилась

### 1. Получите детальные логи:
```bash
# В Render Dashboard → Logs
# Скопируйте последние 100-200 строк
```

### 2. Проверьте локально:
```bash
cd /c/Users/Jhonny/AndroidStudioProjects/Techapp
./gradlew :server:shadowJar --stacktrace
```

### 3. Отправьте логи:
- Скопируйте ошибку из Render
- Или вывод локальной сборки
- Отправьте для анализа

---

## 💡 Полезные команды для отладки

### Проверка файлов в репозитории:
```bash
git ls-files | grep Dockerfile
```

### Просмотр последних коммитов:
```bash
git log --oneline -10
```

### Просмотр изменений в Dockerfile:
```bash
git diff deploy/render/Dockerfile
```

### Проверка статуса перед push:
```bash
git status
git diff --staged
```

---

## 📋 Чеклист при появлении новой ошибки

1. [ ] Открыть Render Dashboard → Logs
2. [ ] Найти красные строки с ошибками
3. [ ] Прокрутить ВВЕРХ/ВНИЗ от ошибки
4. [ ] Скопировать секцию с "FAILURE" или "ERROR"
5. [ ] Проверить, что изменения отправлены на GitHub
6. [ ] Дождаться новой сборки
7. [ ] Проверить логи нового деплоя

---

**Если нужна помощь с конкретной ошибкой - скопируйте логи из Render и отправьте!** 🔍



