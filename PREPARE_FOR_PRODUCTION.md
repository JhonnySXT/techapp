# 🚀 Подготовка к Production и тестированию на Android

## ✅ Что уже сделано:

1. **Конфигурация для dev/prod** (`config.js`)
   - Автоматическое определение окружения
   - Динамический API_BASE и FILE_BASE
   - Поддержка WebSocket для production

2. **PWA манифест** (`manifest.json`)
   - Настройки для установки на Android
   - Иконки и темы
   - Shortcuts для быстрого доступа

3. **Мобильная адаптивность**
   - Адаптивные стили для экранов < 768px и < 480px
   - Оптимизация для touch-устройств
   - Предотвращение зума на iOS (font-size: 16px для input)

4. **Исправлены hardcoded URLs**
   - Все `localhost:8081` заменены на `FILE_BASE` и `API_BASE`
   - Поддержка production URL

## 📋 Что нужно сделать перед развертыванием на Render:

### 1. Создать иконки для PWA

Создайте два файла иконок:
- `icon-192.png` (192x192 пикселей)
- `icon-512.png` (512x512 пикселей)

Можно использовать простой логотип с медведем или текстом "Дача".

### 2. Настроить переменные окружения на Render

В настройках вашего сервиса на Render добавьте:

```
JWT_SECRET=<ваш-секретный-ключ-минимум-32-символа>
PORT=8081
HOST=0.0.0.0
```

**Важно:** `JWT_SECRET` должен быть длинным случайным ключом (минимум 32 символа). 
Можно сгенерировать командой:
```bash
openssl rand -base64 32
```

### 3. Настроить статические файлы на сервере

Убедитесь, что сервер отдает статические файлы:
- `test-app.html`
- `config.js`
- `manifest.json`
- `sw.js`
- `icon-192.png`
- `icon-512.png`

### 4. Проверить CORS настройки

CORS уже настроен на `anyHost()` в `Security.kt`, что подходит для production.

### 5. Настроить базу данных

SQLite файл будет создаваться автоматически. На Render убедитесь, что:
- Есть права на запись в директорию приложения
- База данных сохраняется между перезапусками (используйте persistent storage)

### 6. Тестирование на Android

После развертывания:

1. **Откройте приложение в Chrome на Android**
2. **Установите как PWA:**
   - Меню → "Добавить на главный экран"
   - Или появится баннер "Установить приложение"

3. **Проверьте:**
   - ✅ Авторизация работает
   - ✅ Заявки загружаются
   - ✅ Фотографии отображаются
   - ✅ Офлайн-режим работает
   - ✅ Уведомления работают (нужно разрешение)

### 7. Оптимизация для Android

**Дополнительные улучшения (опционально):**

1. **Добавить splash screen** в `manifest.json`:
```json
"splash_screens": [
  {
    "src": "icon-512.png",
    "sizes": "512x512",
    "type": "image/png"
  }
]
```

2. **Оптимизировать изображения:**
   - Использовать WebP формат для иконок
   - Сжать фотографии перед загрузкой

3. **Добавить мета-теги для лучшей интеграции:**
```html
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-touch-fullscreen" content="yes">
```

## 🔧 Настройка сервера для статических файлов

Если сервер не отдает статические файлы автоматически, добавьте в `Routing.kt`:

```kotlin
staticResources("/", "static") {
    default("test-app.html")
}
```

И создайте директорию `server/src/main/resources/static/` с файлами.

## 📱 Чеклист перед тестированием на телефоне:

- [ ] Сервер развернут на Render
- [ ] Переменные окружения настроены
- [ ] Иконки созданы и загружены
- [ ] Приложение открывается в браузере
- [ ] Авторизация работает
- [ ] API запросы проходят (проверить в DevTools)
- [ ] PWA манифест загружается (`/manifest.json`)
- [ ] Service Worker регистрируется
- [ ] Приложение можно установить на главный экран

## 🐛 Возможные проблемы:

1. **CORS ошибки:**
   - Проверьте, что CORS настроен на `anyHost()`
   - Убедитесь, что фронтенд и бэкенд на одном домене (или настройте конкретные origins)

2. **Фотографии не загружаются:**
   - Проверьте путь к файлам (`/uploads/`)
   - Убедитесь, что директория `uploads` существует и доступна

3. **Service Worker не работает:**
   - Должен быть на HTTPS (или localhost)
   - Проверьте консоль браузера на ошибки

4. **PWA не устанавливается:**
   - Проверьте, что `manifest.json` доступен
   - Убедитесь, что иконки существуют
   - Проверьте консоль на ошибки

## 📝 Следующие шаги:

1. Развернуть сервер на Render
2. Загрузить статические файлы
3. Протестировать на компьютере
4. Протестировать на Android телефоне
5. Собрать обратную связь от пользователей

