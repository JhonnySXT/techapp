# 📝 Точные настройки для Render.com

## 🔧 Docker Build Context

### Ответ: Просто точка `.` (без скобок, без кавычек)

В поле **"Docker Build Context"** напишите:
```
.
```

**Только точка, ничего больше!**

---

## 🔍 Где найти поля для PORT и HOST

### Вариант 1: В форме создания сервиса

1. **Прокрутите форму вниз** (после всех основных полей)
2. Найдите раздел **"Advanced"** или **"Environment Variables"**
3. Нажмите на него, чтобы **развернуть** (может быть свернуто)
4. Там будут поля для переменных окружения

### Вариант 2: Если не видите "Advanced"

1. Ищите раздел **"Environment Variables"** или **"Env Vars"**
2. Или кнопку **"+ Add Environment Variable"** или **"+ Add"**
3. Нажмите на него

### Вариант 3: После создания сервиса

Если уже создали сервис:
1. Откройте ваш сервис в Render Dashboard
2. Перейдите в **"Environment"** или **"Settings"**
3. Найдите раздел **"Environment Variables"**
4. Добавьте переменные там

---

## 📋 Пошаговая инструкция для переменных

### В форме создания сервиса:

1. **Прокрутите вниз** до конца формы
2. Найдите **"Advanced"** или **"Environment Variables"**
3. **Разверните** этот раздел (кликните на него)
4. Вы увидите:
   - Пустые поля или
   - Кнопку **"+ Add Environment Variable"** или **"+ Add"**

5. **Добавьте первую переменную:**
   - Нажмите **"+ Add"** (если есть)
   - В поле **"Key"** напишите: `PORT`
   - В поле **"Value"** напишите: `8081`

6. **Добавьте вторую переменную:**
   - Нажмите **"+ Add"** еще раз
   - В поле **"Key"** напишите: `HOST`
   - В поле **"Value"** напишите: `0.0.0.0`

---

## 🖼️ Как это выглядит в интерфейсе:

```
┌─────────────────────────────────────┐
│ Environment Variables               │
│ ▼ (нажмите, чтобы развернуть)      │
├─────────────────────────────────────┤
│ + Add Environment Variable         │
│                                     │
│ Key:   PORT                         │
│ Value: 8081                        │
│                                     │
│ + Add Environment Variable         │
│                                     │
│ Key:   HOST                         │
│ Value: 0.0.0.0                     │
└─────────────────────────────────────┘
```

---

## ⚠️ Важно:

### Docker Build Context:
- ✅ Правильно: `.` (просто точка)
- ❌ Неправильно: `(.)` или `"."` или `'.'` или что-то еще

### Переменные окружения:
- Если не видите раздел - **прокрутите форму вниз**
- Ищите **"Advanced"** или **"Environment Variables"**
- Может быть **свернуто** - нажмите, чтобы развернуть

---

## 🎯 Чеклист полей:

- [ ] **Docker Build Context:** `.` (просто точка)
- [ ] **Dockerfile Path:** `deploy/render/Dockerfile`
- [ ] **Environment Variables:**
  - [ ] Нашел раздел "Advanced" или "Environment Variables"
  - [ ] Развернул раздел
  - [ ] Добавил `PORT` = `8081`
  - [ ] Добавил `HOST` = `0.0.0.0`

---

## 💡 Если все еще не можете найти:

**Альтернатива:** Можно добавить переменные ПОСЛЕ создания сервиса:

1. Создайте сервис БЕЗ переменных (можно добавить позже)
2. После создания откройте ваш сервис
3. Перейдите в **"Environment"** или **"Settings"**
4. Найдите **"Environment Variables"**
5. Добавьте `PORT` и `HOST` там

**Но лучше добавить сразу при создании!**

---

**Попробуйте прокрутить форму вниз и найти "Advanced" - там точно будут переменные окружения!** 🚀










