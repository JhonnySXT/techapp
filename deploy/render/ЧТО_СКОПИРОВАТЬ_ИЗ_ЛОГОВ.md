# 📋 Что скопировать из логов Render

## 🔍 Где найти конкретную ошибку:

В логах Render **прокрутите ВВЕРХ** от красной строки с ошибкой.

**Ищите строки, которые содержат:**

### 1. Ошибки Gradle:
```
FAILURE: Build failed with an exception.

* What went wrong:
...
* Try:
...
```

### 2. Ошибки задач:
```
> Task :server:shadowJar FAILED
```

### 3. Ошибки зависимостей:
```
Could not resolve: ...
Could not find: ...
```

### 4. Ошибки компиляции:
```
e: file:///app/server/src/.../SomeFile.kt:XX:XX
Unresolved reference: ...
```

### 5. Ошибки конфигурации:
```
Plugin [id: '...'] was not found
```

---

## 📝 Что скопировать:

**Скопируйте:**
1. **Все строки с "FAILURE"** или "ERROR"
2. **Секцию "What went wrong"** (если есть)
3. **Последние 30-50 строк** перед Java stack trace
4. **Все красные строки** (обычно это ошибки)

**Пример того, что нужно:**
```
FAILURE: Build failed with an exception.

* What went wrong:
Task :server:shadowJar failed
...
* Try:
Run with --info or --debug option to get more log output.
```

---

## 🎯 Как скопировать:

1. **В логах Render** прокрутите вверх
2. **Найдите секцию с ошибкой** (обычно перед Java stack trace)
3. **Выделите текст** (мышкой или Ctrl+A)
4. **Скопируйте** (Ctrl+C)
5. **Вставьте сюда** в чат

---

## 💡 Альтернатива:

Если не можете найти конкретную ошибку:
1. **Скопируйте последние 50-100 строк** логов
2. **Отправьте мне** - я найду ошибку

---

**Прокрутите логи ВВЕРХ и найдите строки с "FAILURE" или "ERROR" - это конкретная ошибка Gradle!** 🔍

